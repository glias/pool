import { SettingOutlined } from '@ant-design/icons';
import { Button } from 'antd';
import React from 'react';

export const SettingButton: React.FC = () => {
  return <Button icon={<SettingOutlined />} />;
};
